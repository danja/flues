{
  "name": "flues-web-server",
  "version": "0.1.0",
  "description": "Web UI server for flues-synth (MIDI bridge + audio tap)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["flues", "synth", "midi", "websocket", "alsa"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "fft.js": "^4.0.3",
    "easymidi": "^3.0.1"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
