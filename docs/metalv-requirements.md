# MetaLV

MetaLV will be an lv2 plugin that acts as both an lv2 plugin and an lv2 host. It also acts as an mcp server so AI agents can communicate with any plugins MetaLV is hosting. As a plugin it will follow the patterns of other plugins in the repo such as p-mix. As a host there is /home/danny/github/jalv for reference. 